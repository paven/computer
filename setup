#BREW
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    echo >> /Users/pg/.zprofile
    echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> /Users/pg/.zprofile
    eval "$(/opt/homebrew/bin/brew shellenv)"

#SYSTEM
# To allow a specific unsigned app (Gatekeeper disabled UI on macOS Tahoe+):
# xattr -rd com.apple.quarantine /Applications/SomeApp.app

#MAS
brew install mas

#computer && dump
git clone git@github.com:paven/computer.git
cd computer/
./install
